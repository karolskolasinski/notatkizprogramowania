import React from 'react';
import './Loader.css';

const Loader = () => (
    <main className={'main'}>
        <div className="spinner-wrapper">
            <div className="spinner" />
        </div>
    </main>
);

export default Loader;


